# Fine-Tune Ready

Purpose: Breeze-ASR-25/Hugging Face-ready dataset views and manifests.

Files classified here: 4982
Folder links created: 1

Use `INDEX.tsv` for the full file-level catalog. Use `links/` for convenient entry points back to the original data folders.

For current Whisper ASR work, prefer `../../60_whisper_asr_finetuning/datasets/janus_165_v1/`; it points to this fine-tune-ready dataset through stable repo-relative links.

This directory is an organization overlay; linked folders point back to the original JANUS export and should be treated as the source location.
