# Runtime Environments

Purpose: Python virtual environments, installed packages, compiled libraries, caches, binaries, and dependency artifacts.

Files classified here: 105548
Folder links created: 43

Use `INDEX.tsv` for the full file-level catalog. Use `links/` for convenient entry points back to the original data folders.

This directory is an organization overlay; linked folders point back to the original JANUS export and should be treated as the source location.

These archived runtime folders are not the active repo environment. Rebuild the
top-level `.venv/` from `../../requirements-whisper.txt` for current Whisper
experiments.
