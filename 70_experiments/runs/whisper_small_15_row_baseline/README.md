# Run Record: whisper_small_15_row_baseline

## Summary

- Status: completed
- Date: 2026-05-25
- Owner: Jason Lin
- Config: `60_whisper_asr_finetuning/configs/janus-15-asr-model-candidates.yaml`
- Dataset: `janus_165_v1`
- Model: `openai/whisper-small`
- Seed: `165`
- Hardware: local RTX 5080 CUDA with cuDNN disabled

## Purpose

Generate the first full 15-row Whisper-family ASR hypothesis file for the
reviewed JANUS gold subset. This is a model-comparison input, not a training
run and not a final quality claim.

## Inputs

- Pilot manifest:
  `40_breeze_asr25_finetune_dataset/manifests/nemo_pilot_input_manifest.jsonl`
- Gold review:
  `40_breeze_asr25_finetune_dataset/reports/gold_subset_review.tsv`
- Base checkpoint: `openai/whisper-small`
- Previous run: `whisper_small_smoke_test`

## Inference Settings

- Runtime: CUDA with `--disable-cudnn` on this workstation.
- Language/task: `zh` / `transcribe`
- Max samples: `15`
- Max new tokens: `225`
- Label mode: `heuristic_v0`

## Results

| Split | CER | WER | Loss | Notes |
| --- | ---: | ---: | ---: | --- |
| janus_15_pilot_15 | 53.08 | 500.0 | n/a | CUDA inference with `--disable-cudnn`; WER is not meaningful for unsegmented Chinese text. |

## Observations

- Raw predictions stay under ignored `predictions/`.
- The `asr_label` column is generated by a small pilot-only keyword heuristic
  so SRES/CEIS/downstream scripts can be wired before a learned routing model
  exists.
- `validate_janus_asr_hypotheses.py --require-labels --require-quality-signal`
  passed for all 15 expected `audio_id` values.
- Do not interpret this run as a full ASR benchmark.

## Failure Or Risk Notes

- CUDA/cuDNN inference currently fails on this workstation with a cuDNN
  sublibrary version mismatch, matching the earlier NeMo CUDA failure. PyTorch
  CUDA convolution passes when cuDNN is disabled, so this run should use
  `--runtime cuda --disable-cudnn`.
- The heuristic `asr_label` is for metric plumbing and should be spot-checked
  only when it changes a decision-critical row.

## Artifacts

- Metrics: `metrics.csv`
- Raw predictions: `predictions/` (ignored local)
- Summary: `artifacts/` (ignored local)
