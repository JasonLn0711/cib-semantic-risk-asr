# Optional runtime dependencies for non-Whisper ASR candidate gates.
#
# Keep this separate from requirements-whisper.txt because these packages update
# the local Transformers stack and should be installed intentionally.
funasr==1.3.3
modelscope==1.37.1
qwen-asr==0.0.6

# Gemma 4 audio is not pinned here yet. The 2026-05-25 local gate found that
# installed Transformers 4.57.6 does not expose AutoModelForMultimodalLM, while
# the Gemma 4 model configs declare a 5.5.0.dev0-era runtime.
